{
  "name": "portfolio-contact-backend",
  "version": "1.0.0",
  "private": true,
  "description": "API de contacto lista para conectar la landing del portafolio a un envio real.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/config.js && node --check src/routes/contact.js && node --check src/routes/assistant.js && node --check src/lib/email-service.js && node --check src/lib/rate-limit.js && node --check src/lib/validate-contact.js && node --check src/lib/validate-assistant.js && node --check src/lib/assistant-service.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "helmet": "^8.1.0"
  }
}
